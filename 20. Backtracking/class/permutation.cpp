#include <iostream>
#include <string>
#include <vector>
using namespace std;

void permutation(string str, string ans) {
     
    for (int i = 0; i < str.size(); i++){
        char ch = str[i];
        string nextStr = str.substr(0,)
    }
}

int main() {
    string str = "abc";
    string ans = "";

    return 0;
}